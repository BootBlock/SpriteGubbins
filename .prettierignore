# Build output & deps
dist
dist-ssr
coverage
node_modules

# Lockfiles & data
package-lock.json
*.sqlite
*.db

# Prose & infra: keep the code formatter out of hand-authored docs, markup, and config
# YAML/HTML (reflowing these churns carefully hand-wrapped content to no benefit).
*.md
*.yaml
*.yml
*.html
