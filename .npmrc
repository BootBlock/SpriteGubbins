# npm ignores `engines` in package.json unless this is set. With it, `npm install` and `npm ci`
# refuse a Node or an npm below that floor and name both versions, rather than an npm older than
# 11.6.3 silently rewriting package-lock.json. tests/npm-toolchain.test.ts says why.
engine-strict=true
