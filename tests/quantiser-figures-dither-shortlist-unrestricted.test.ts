import { ditherShortlistSuite } from './ditherShortlistSuite.ts';

// The unrestricted column, alone because it costs more than the other five together. The suite,
// and what it asserts, is in `ditherShortlistSuite.ts`.
ditherShortlistSuite([128]);
