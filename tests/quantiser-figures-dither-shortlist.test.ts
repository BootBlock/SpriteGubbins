import { ditherShortlistSuite } from './ditherShortlistSuite.ts';

// The five shortlist columns, the one the constant ships among them. The suite, and what it
// asserts, is in `ditherShortlistSuite.ts`.
ditherShortlistSuite([2, 3, 4, 6, 8]);
